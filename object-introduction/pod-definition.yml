apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    name: myapp-pod
    app: myapp
    type: pod

spec: 
    containers:     #list
      - name: nginx-container
        image: nginx
#      - name: backend-container
#        image: redis
